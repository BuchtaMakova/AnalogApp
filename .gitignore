# Secrets — never commit real credentials. docker-compose.yml and appsettings.json only ever
# hold safe local defaults (MinIO, mock AI); real keys belong in your own shell/environment.
.env
.env.local
.env.*.local
*.pfx

# .NET
backend/**/bin/
backend/**/obj/
*.user
.vs/

# Node / Vite
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Editors / OS
.DS_Store
Thumbs.db
.idea/
